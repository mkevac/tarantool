#ifdef ENABLE_DTRACE
#include "coro_dtrace.h"
#ifdef ENABLE_DTRACE_BUILTIN
#define	CORO_END() TARANTOOL_DONE()
#define	CORO_END_ENABLED() TARANTOOL_DONE_ENABLED()
#define	CORO_INIT() TARANTOOL_INIT()
#define	CORO_INIT_ENABLED() TARANTOOL_INIT_ENABLED()
#define	CORO_START() TARANTOOL_START()
#define	CORO_START_ENABLED() TARANTOOL_START_ENABLED()
#endif
#else
#define	CORO_END()
#define	CORO_END_ENABLED() (0)
#define	CORO_INIT()
#define	CORO_INIT_ENABLED() (0)
#define	CORO_START()
#define	CORO_START_ENABLED() (0)
#endif
